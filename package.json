{
    "name": "phasehumans",
    "type": "module",
    "private": true,
    "scripts": {
        "prepare": "husky",
        "format": "prettier --write .",
        "format:check": "prettier --check ."
    },
    "devDependencies": {
        "@playwright/test": "^1.58.2",
        "@types/bun": "latest",
        "husky": "^9.1.7",
        "prettier": "^3.8.1"
    },
    "peerDependencies": {
        "typescript": "^6.0.2"
    }
}
