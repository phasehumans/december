import React from 'react'

interface ReviewPageProps {
    onNewProject: () => void
}

export const ReviewPage: React.FC<ReviewPageProps> = () => {
    return (
        <div className="relative h-full w-full flex-1 bg-background font-sans">
            {/* Empty Review Page */}
        </div>
    )
}
